Export both frames from Figma as PNG at 2x, then save them here with
these exact filenames:

  where-it-breaks.png   <- "Diagram 1 — Where it breaks"
  same-prompt.png       <- "Diagram 2 — Same prompt, different answer"

Figma file:
https://www.figma.com/design/3e5f8VZIKmy4RFy4wQYz0a

In Figma: select the frame, right panel > Export > PNG > 2x > Export.
